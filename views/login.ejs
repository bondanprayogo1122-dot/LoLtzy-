<%- include('partials/head', { pageTitle: pageTitle }) %>
<%- include('partials/header') %>

<section class="flex items-center justify-center min-h-[70vh]">
  <div class="w-full max-w-md rounded-2xl bg-white/5 border border-white/10 shadow-lg backdrop-blur-xl p-6">
    <h1 class="text-2xl font-bold text-center mb-6">Login</h1>
    <form action="/login" method="post" class="space-y-4">
      <div>
        <label class="block text-sm mb-1">Email</label>
        <input
          type="email"
          name="email"
          required
          class="w-full px-3 py-2 rounded-lg bg-white/10 border border-white/10 placeholder:text-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-400"
          placeholder="you@example.com"
        />
      </div>
      <div>
        <label class="block text-sm mb-1">Password</label>
        <input
          type="password"
          name="password"
          required
          class="w-full px-3 py-2 rounded-lg bg-white/10 border border-white/10 placeholder:text-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-400"
          placeholder="••••••••"
        />
      </div>
      <button
        type="submit"
        class="w-full py-2 rounded-lg bg-indigo-600 hover:bg-indigo-700 transition text-white font-semibold"
      >
        Login
      </button>
    </form>
    <p class="text-center text-sm text-gray-400 mt-4">
      Belum punya akun? <a href="/register" class="text-indigo-400 hover:text-indigo-300">Register</a>
    </p>
  </div>
</section>

<%- include('partials/footer') %>
<%- include('partials/end') %>
